<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ethioorigins">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tour" android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".AccountSettings"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HotelDetailActivity"
            android:parentActivityName=".Hotel" />
        <activity
            android:name=".Hotel"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".About" />
        <activity
            android:name=".DetailRestaurants"
            android:label="@string/title_activity_detail_restaurants"
            android:parentActivityName=".Restaurants"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Restaurants"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>